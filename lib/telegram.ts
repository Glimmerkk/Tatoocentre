import {env} from "cloudflare:workers";
type TelegramResult={ok:boolean;result?:{message_id:number}};
export async function sendTelegram(text:string){const token=env.TELEGRAM_BOT_TOKEN as string|undefined,chatId=env.TELEGRAM_ADMIN_CHAT_ID as string|undefined,secret=env.TELEGRAM_WEBHOOK_SECRET as string|undefined;if(!token||!chatId||!secret)throw new Error("Telegram is not configured");await fetch(`https://api.telegram.org/bot${token}/setWebhook`,{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify({url:"https://ink-noir-tattoo-studio.mia-chloe-1966.chatgpt.site/api/telegram/webhook",secret_token:secret,allowed_updates:["message"]})});const r=await fetch(`https://api.telegram.org/bot${token}/sendMessage`,{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify({chat_id:chatId,text,disable_web_page_preview:true,reply_markup:{force_reply:true,input_field_placeholder:"Reply to this customer"}})});const data=await r.json() as TelegramResult;if(!data.ok||!data.result)throw new Error("Telegram delivery failed");return data.result.message_id}
